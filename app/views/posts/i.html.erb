<%= form.label :search, "Search by post name:",
  class: "block mb-2" %>
  <div class="flex space-x-3">
    <%= form.text_field :search, class: "py-2 px-5
    form-input clearfix rounded border ring-0 focus:ring-4
    focus:ring-orange-100 focus:shadow-none
    dark:text-slate-600 focus:border-orange-500 focus:outline-none",
    data: {action: "input->search-form#search"} %>

    <%= form.submit 'Search', class: "px-4 py-2 font-medium
    bg-orange-400 text-neutal-900 rounded flex items-center
    cursor-pointer hover:bg-orange-600 focus:ring-4 ring-0
    text-gray-900 hover:text-gray-100 focus:ring-orange-100" %>
  </div>
<% end %>
<%= link_to image_tag(post.image,
style:"width:15em; heigh:auto"), post.image %>
<%= link_to image_tag(@posts.first.image,
style:"width:auto; heigh:auto"),
@posts.first.image,
data: { action: 'click->dark-mode#toggle'} %>
<% if @posts.first.image.attached? %>
  <%= link_to image_tag(@posts.first.image,
  style:"width:auto; heigh:auto"),
  @posts.first.image,
  data: { action: 'click->dark-mode#toggle'} %>
<% end %>
<br>
<div data-controller="dark-mode">
<%= image_tag("Park.svg",
data: { action: 'click->dark-mode#toggle'}) %>
<%= image_tag("svjetlost1.svg",
data: { action: 'click->dark-mode#toggle'}) %>
<br>
<%== pagy_nav(@pagy) %>
